version: "3.0"
services:
    application:
        container_name: application
        build:
            context: .
            dockerfile: ./Dockerfile
        ports:
            - ${PORT}:${PORT}
        env_file:
            - .env
        restart: always
        volumes:
            - .:/app
            - /app/node_modules
            - /app/dist
